{
  "name": "maha-tribal-intelligence-grid",
  "version": "1.0.0",
  "private": true,
  "description": "Maha Tribal Intelligence Grid — frontend-only, fully offline government-grade dashboard (no backend, no API). All data is generated in-browser and Simulated for PoC.",
  "scripts": {
    "install:all": "npm --prefix frontend install",
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview"
  }
}
